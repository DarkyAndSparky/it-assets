<div align="center">

# 🖥️ IT Assets

**Система внутреннего учёта ИТ-оборудования**

![Version](https://img.shields.io/badge/версия-β1·26w27·02-blue?style=flat-square)
![Node](https://img.shields.io/badge/Node.js-v18%2B-brightgreen?style=flat-square&logo=node.js)
![License](https://img.shields.io/badge/лицензия-внутренняя-lightgrey?style=flat-square)
![Platform](https://img.shields.io/badge/платформа-Windows%20|%20Linux%20|%20macOS-informational?style=flat-square)
![Branch](https://img.shields.io/badge/ветка-main%20(production)-success?style=flat-square)

Лёгкое self-hosted приложение для инвентаризации ноутбуков, серверов,
сетевого оборудования и периферии.
Работает без интернета, не требует установки БД — всё хранится в JSON.

> **Это production-ветка.** Здесь только стабильный, проверенный код.
> Разработка ведётся в ветке [`dev`](https://github.com/DarkyAndSparky/it-assets/tree/dev).

</div>

---

## ✨ Возможности

- 📋 Три реестра: **ОС / Мелочи / Инфра**
- 🔄 История перемещений и изменений с фильтрацией
- 👥 Сотрудники, организации, филиалы, локации
- 📥 Импорт из CSV, экспорт в Excel
- 🔐 Три роли: Администратор · Оператор · Просмотр
- 📦 Автоматическое резервное копирование (4 пула)
- 🔒 HTTPS из коробки (самоподписанный сертификат)
- 📡 Работа в локальной сети без настройки
- 🛡️ Rate-limiting, bcrypt PIN, security-заголовки

---

## 🚀 Быстрый старт

### Windows

1. Установите **[Node.js LTS](https://nodejs.org)**
2. Дважды кликните **`INSTALL.bat`** — установит зависимости
3. Дважды кликните **`START.bat`** — откроет браузер

### Linux / macOS

```bash
chmod +x install.sh start.sh
./install.sh   # установить зависимости
./start.sh     # запустить
```

Приложение: **`https://localhost:3443`**

> ⚠️ Браузер покажет предупреждение о сертификате — это ожидаемо.
> Нажмите «Дополнительно» → «Перейти на сайт».

---

## 🔑 Вход по умолчанию

| Логин   | Пароль     | Роль          |
|---------|------------|---------------|
| `admin` | `admn0000` | Администратор |

> ⚠️ **Смените пароль после первого входа** — система напомнит сама.

### Роли и права

|                       | Администратор | Оператор | Просмотр |
|-----------------------|:---:|:---:|:---:|
| Просмотр оборудования | ✅ | ✅ | ✅ |
| Добавить / изменить   | ✅ | ✅ | ❌ |
| Настройки системы     | ✅ | ❌ | ❌ |
| Управление бэкапами   | ✅ | ❌ | ❌ |

---

## 📋 Требования

|         | Минимум  | Рекомендуется |
|---------|----------|---------------|
| Node.js | v18 LTS  | v20+ LTS      |
| ОЗУ     | 128 MB   | 256 MB        |
| Место   | 50 MB    | 200 MB        |

**Установка Node.js на Linux:**

```bash
# Ubuntu / Debian
sudo apt update && sudo apt install nodejs npm

# Через nvm (рекомендуется)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
nvm install --lts
```

---

## 🗂️ Структура проекта

```
it-assets/
├── server/
│   ├── index.js              # веб-сервер (Express + HTTPS)
│   ├── database.js           # работа с данными (lowdb/JSON)
│   ├── migrate.js            # автомиграция схемы при старте (v1→v7)
│   ├── pin.js                # хеширование PIN (bcrypt)
│   ├── cert.js               # генерация TLS-сертификата
│   ├── middleware/
│   │   ├── auth.js           # аутентификация
│   │   └── rateLimit.js      # защита от перебора
│   ├── repositories/         # слой доступа к данным
│   └── routes/               # маршруты API
├── public/
│   └── index.html            # весь фронтенд (SPA без сборки)
├── data/                     # ← создаётся автоматически, в git не попадает
│   ├── db.json               # реестр оборудования и история
│   ├── config.json           # пользователи, орги, настройки
│   └── backups/              # автоматические резервные копии
├── START.bat / start.sh
├── INSTALL.bat / install.sh
└── package.json
```

---

## ⚙️ Переменные окружения

| Переменная           | По умолчанию | Описание                                         |
|----------------------|--------------|--------------------------------------------------|
| `PORT`               | `3000`       | HTTP-порт (редирект на HTTPS)                    |
| `HTTPS_PORT`         | `3443`       | HTTPS-порт приложения                            |
| `IT_ASSETS_DATA_DIR` | `./data`     | Путь к папке с данными                           |
| `TRUST_PROXY`        | —            | Установите `1` при работе за nginx/reverse proxy |
| `CORS_ORIGINS`       | —            | Разрешённые origins через запятую                |

Пример с кастомными портами:

```bash
PORT=8080 HTTPS_PORT=8443 node server/index.js
```

---

## 🌐 Доступ из локальной сети

Сервер слушает на `0.0.0.0`. Узнайте свой IP:

```bash
# Linux
hostname -I | awk '{print $1}'

# macOS
ipconfig getifaddr en0

# Windows (в cmd)
ipconfig
```

Откройте коллегам: **`https://192.168.x.x:3443`**

**Открыть порт в firewall:**

```bash
# ufw (Ubuntu/Debian)
sudo ufw allow 3443/tcp

# firewalld (Fedora/RHEL)
sudo firewall-cmd --add-port=3443/tcp --permanent && sudo firewall-cmd --reload
```

---

## 💾 Резервное копирование

Автобэкап встроен — копии сохраняются в `data/backups/`:

| Тип                        | Хранится     |
|----------------------------|--------------|
| Авто (каждый час)          | последние 20 |
| При старте сервера         | последние 10 |
| Ручной (через интерфейс)   | последние 20 |
| Перед восстановлением      | последние 5  |

Ручное копирование:

```bash
cp data/db.json     ~/backup/db_$(date +%Y%m%d).json
cp data/config.json ~/backup/config_$(date +%Y%m%d).json
```

---

## 📥 Импорт из CSV

1. Подготовьте файл с заголовками:

```
Вкладка, Инв. номер, Филиал, Расположение, Ответственный, Тип, Модель,
Серийный №, Статус, Организация, Примечание, MAC, IP, Hostname, Прошивка
```

2. ⚙️ Настройки → **Импорт из CSV** → выберите файл → **Импортировать**

Дубли по серийному номеру пропускаются автоматически.

---

## 🛠️ Устранение проблем

| Проблема                   | Решение                                                   |
|----------------------------|-----------------------------------------------------------|
| `node: command not found`  | Установите Node.js                                        |
| Белый экран                | F12 → Console — скопируйте ошибку                         |
| Порт занят (`EADDRINUSE`)  | Задайте другой `PORT` / `HTTPS_PORT`                      |
| Коллеги не видят сервер    | Проверьте firewall, используйте `https://` и порт `3443`  |
| «Соединение не защищено»   | Ожидаемо — нажмите «Дополнительно» → «Перейти»            |
| `npm install` падает       | Попробуйте `npm install --legacy-peer-deps`               |

---

## 🔒 Безопасность

| Уязвимость                                       | Статус        |
|--------------------------------------------------|---------------|
| Утечка stack trace на 500-ошибках                | ✅ закрыто    |
| PIN хранился открытым текстом                    | ✅ bcrypt      |
| Rate-limit обходился подделкой `X-Forwarded-For` | ✅ закрыто    |
| Отсутствие security-заголовков                   | ✅ добавлены  |
| Раскрытие фреймворка (`X-Powered-By`)            | ✅ отключено  |
| Нет предупреждения о дефолтном пароле            | ✅ баннер     |

---

## 🌿 Ветки и релизы

| Ветка  | Назначение                                    |
|--------|-----------------------------------------------|
| `main` | Production — стабильный чистый билд           |
| `dev`  | Разработка — тесты, эксперименты, новые фичи  |

Релизы выходят из `dev` в `main` после прохождения всех тестов (237 тестов, Jest).

**Последний релиз:** `beta-1-26w27-02`

---

## 🛠️ Технологии

| Слой             | Стек                              |
|------------------|-----------------------------------|
| Сервер           | Node.js + Express                 |
| База данных      | lowdb (JSON-файл, без установки)  |
| Аутентификация   | Header-based, bcrypt PIN          |
| TLS              | selfsigned (авто-генерация)       |
| Фронтенд         | Vanilla JS, SPA без сборки        |

---

<div align="center">

Разработано для внутреннего учёта ИТ-оборудования
Автор: **[DarkyAndSparky](https://github.com/DarkyAndSparky)**

</div>
