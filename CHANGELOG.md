# Changelog

Формат по мотивам [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Подробный бэклог и статусы задач — во внешнем файле `ROADMAP-CONSOLIDATED.md`
(не в репозитории, обновляется отдельно). Здесь — краткая хронология того,
что реально попало в `dev`.

## [Unreleased]

### Added
- Доработка фото активов (по дополнительному запросу пользователя):
  секция фото в карточке актива теперь свёрнута по умолчанию — кнопка
  «Показать фото (N)» разворачивает и лениво грузит миниатюры (байты
  изображений раньше грузились сразу при каждом открытии карточки, даже
  если пользователь не собирался их смотреть). В таблице активов —
  значок 📷 рядом с моделью (только если у актива есть фото, с числом
  если больше одного), клик открывает лайтбокс сразу, без открытия полной
  карточки. Лайтбокс теперь с навигацией ‹ › между фото (актуально для
  составных вещей с несколькими фото — разные ракурсы, комплекты).
  Backend: `listAssets()` (`assets.repo.js`) отдаёт `photo_count` на
  каждый элемент страницы — один сгруппированный SQL-запрос по `IN(...)`
  на всю страницу (не N+1 на каждую строку). Проверено сквозным тестом:
  `photo_count` корректен и для активов с фото, и без (`0`, не `null`).
- Фото активов — можно прикрепить к карточке актива фотографии (шильдик
  с серийным номером/IMEI, характеристики на этикетке и т.п.), сделанные
  телефоном. Загружаются как base64 data URL (тот же паттерн, что уже
  использовался для логотипа компании — не понадобилось тащить
  multer/busboy для multipart). Метаданные в новой таблице SQLite
  `asset_photos`, файлы — на диске под `DATA_DIR/attachments/<asset_id>/`
  (права `600` благодаря OPS-2). Лимиты: 8MB на файл, 20 фото на актив,
  форматы JPEG/PNG/WebP/HEIC. `GET`-роуты (список/сама картинка) —
  `requireLogin` (viewer видит), `POST`/`DELETE` — `requireAuth` (edit-
  режим). Фото включены в ZIP-бэкапы (`makeBackup`/`restoreBackup` в
  `backup.routes.js`) — иначе молча терялись бы при восстановлении.
  Фронтенд: миниатюры грузятся через `fetch()`+blob URL (не голый
  `<img src>` — наша header-based авторизация не проходит через обычный
  тег `<img>`), лайтбокс на клик, кнопка «Сфотографировать» с
  `capture="environment"` для мобильной камеры. Проверено сквозным тестом
  на реальном сервере: полный цикл upload→list→download→delete, права
  viewer/operator, валидация (плохой data URL, чужой MIME, несуществующий
  актив), и отдельно — полный цикл backup→удаление файла вручную→restore
  подтвердил, что фото восстанавливается корректно.

### Fixed
- OPS-5 (Track 9, найдено при аудите net-monitor): несуществующий
  `/api/...` путь (опечатка, устаревший вызов после рефакторинга)
  проваливался в SPA-фоллбек и получал HTML `index.html` вместо честного
  404 — фронтенд, ожидающий `.json()`, падал с непонятной ошибкой парсинга.
  Добавлен `app.use('/api', (req,res)=>res.status(404).json({error:'not_found'}))`
  перед `app.get('*', ...)`. Проверено сквозным тестом: несуществующий
  `/api`-путь → JSON `{"error":"not_found"}`, существующие `/api`-эндпоинты
  не задеты (`/api/health` → 200, защищённый без авторизации → 401),
  не-`/api`-пути (SPA-роуты) по-прежнему получают HTML.
- SEC-13 (Track 9, найдено при аудите net-monitor): не было заголовка
  `Strict-Transport-Security` — мы всегда работаем по HTTPS
  (самоподписанный сертификат), HSTS был к месту. Добавлен условно на
  `req.secure` (не безусловно) — не отправляется на редком фолбэк-пути
  (если TLS не поднялся, сервер отвечает по голому HTTP), чтобы не
  утверждать «всегда HTTPS», когда это не так. `req.secure` не подвержен
  подмене через заголовки в нашей конфигурации — `trust proxy` не
  настроен через `app.set()` (Express), `TRUST_PROXY` проверяется вручную
  только в `rateLimit.js`. Проверено сквозным тестом: заголовок реально
  присутствует на HTTPS-ответах, отсутствует на HTTP-редиректе (отдельный
  `httpApp`, middleware до него не доходит).
- OPS-4 (Track 9, найдено при аудите net-monitor): лимитирован был только
  `/api/users/login` — остальные эндпоинты (включая публично читаемые
  после INFRA-7 `requireLogin`-роуты и полностью публичные
  `/api/stats`/`/api/history`) не были защищены от злоупотребления вообще.
  Добавлена зависимость `express-rate-limit@^8.7.0`, новый
  `server/middleware/apiRateLimit.js` — два профиля (read 300/мин, write
  60/мин на IP), смонтирован глобально на `/api/*`. `/api/health`
  (публичный Docker HEALTHCHECK) явно исключён. Отключается через
  `RATE_LIMIT_DISABLED=1` или автоматически при `NODE_ENV=test` (иначе
  supertest-тесты падали бы из-за самого факта частых вызовов в цикле, не
  из-за реального бага). Проверено сквозным тестом на реальном сервере:
  первый `429` наступил ровно на 300-м запросе (совпадает с лимитом),
  `/api/health` — 350 запросов, ни одного `429`; отдельно — 400 запросов
  при `NODE_ENV=test` дали 0 `429` (тесты не пострадают).
- OPS-3 (Track 9, найдено при аудите net-monitor + procure-it): не было
  ни одного глобального `unhandledRejection`/`uncaughtException` —
  необработанный promise-reject ронял процесс Node-дефолтом (с версии 15),
  необработанное исключение — вообще без единого лога о причине. Источники
  предлагали противоположные философии (net-monitor: логировать и
  продолжать оба случая; procure-it: завершаться после обоих, следуя
  официальной рекомендации Node.js). Решение принято осознанно, не
  скопировано целиком ни у кого: `unhandledRejection` — логируем, НЕ
  завершаемся (обычно восстановимо, у нас и так мало async-хендлеров);
  `uncaughtException` — завершаемся через уже готовый `gracefulShutdown()`
  (OPS-1) — у нас есть `restart: unless-stopped`, безопасное завершение
  обходится почти бесплатно. Логика проверена изолированным тестом с тем
  же кодом обработчиков: `unhandledRejection` подтверждённо не завершает
  процесс, `uncaughtException` подтверждённо вызывает выход.
- OPS-2 (Track 9, найдено при аудите net-monitor): файлы данных (`db.json`,
  `config.json`, `it-assets.sqlite` — содержит хеши паролей/PIN, логи,
  ZIP-бэкапы) создавались с правами по умолчанию ОС (обычно `644` на
  Linux — читаемо любым локальным пользователем на той же машине).
  Добавлен `process.umask(0o077)` первой строкой `server/index.js`, до
  всех `require()` — теперь всё создаётся с правами `600`/`700`, только
  владелец процесса. Побочный эффект: `cert.pem` (раньше осознанно `644`
  в `cert.js`) тоже станет `600` — не проблема, сертификат отдаётся
  браузерам через TLS handshake, не через файловый доступ. Проверено
  сквозным тестом: реальный запуск сервера, `ls -la` на диске подтвердил
  `600` на всех файлах данных и `700` на `logs/`.
- OPS-1 (Track 9, найдено при аудите procure-it): сервер никак не
  обрабатывал `SIGTERM`/`SIGINT` — `docker stop`/Ctrl+C убивали процесс
  SIGKILL'ом после истечения grace period (Docker-дефолт 10 сек), без
  штатного закрытия SQLite. Добавлен `gracefulShutdown()` в
  `server/index.js`: закрывает слушающие сокеты (не рвёт открытые
  соединения на середине), делает `PRAGMA wal_checkpoint(TRUNCATE)`,
  закрывает SQLite, затем выходит; таймаут-предохранитель 5с на случай
  зависшего keep-alive-соединения; защита от повторного сигнала во время
  shutdown. Добавлен `stop_grace_period: 15s` в `docker-compose.yml`.
  Проверено сквозным тестом: реальный запуск сервера → реальный
  `SIGTERM`/`SIGINT` → штатное закрытие БД → чистый выход, без SIGKILL,
  за &lt;2 сек (было бы 10 сек висения перед принудительным убийством).
- SEC-12 (Track 9, найдено при аудите net-monitor): timing-атака на
  `POST /api/users/login` — `authByLogin()` возвращал `null` мгновенно при
  отсутствии пользователя, не запуская bcrypt-сравнение (которое реально
  отрабатывает при существующем логине с неверным паролем, ~65-70мс).
  Разница во времени ответа позволяла перебором отличать существующие
  логины от несуществующих, даже при одинаковом тексте ошибки. Добавлен
  `DUMMY_HASH` (реальный bcrypt-хеш случайной строки, считается один раз
  при старте модуля) — при отсутствии пользователя сверка с ним всё равно
  выполняется, для тайминга. Измерено: разница до фикса ~66мс, после — <1мс.
- SEC-11 (Track 9, найдено при аудите net-monitor — см. `NETMONITOR-AUDIT.md`):
  CSV-экспорт (`server/repositories/csv.repo.js`, `exportCsv()`) не защищал
  от formula/CSV injection — значение поля, начинающееся с `=`/`+`/`-`/`@`
  (например `=HYPERLINK("http://evil.com","клик")` в «Ответственный» или
  «Примечание»), исполнялось бы как формула при открытии в Excel несмотря
  на кавычки вокруг значения (кавычки защищают только структуру CSV, не
  формулы). Добавлена функция `csvCell()` — префиксует `'` перед опасными
  символами (рекомендация OWASP), применяется ко всем полям экспорта.

### Added
- Единый источник правды по версии перенесён из `package.json` в отдельный
  файл `VERSION` в корне (plain text, формат `<alpha|beta>-N-YYwWW-NN`) —
  идея подсмотрена в соседнем проекте atlas-server. `package.json`,
  README-бейдж и `docs/index.html` теперь синхронизируются ОТ него через
  `scripts/sync-version.js` (переписан под новый источник), а не наоборот.
  `START.bat`/`start.sh` тоже читают версию напрямую из `VERSION`.
- Консольный баннер при старте сервера обрамлён рамкой (`─`×60) по образцу
  atlas-server/printBanner — вся прежняя диагностика (проверка прав на
  запись, размер БД, самопроверка записи SQLite) сохранена без изменений,
  только визуально оформлена.
- Docker: `HEALTHCHECK` в `Dockerfile` и явный `healthcheck:` в
  `docker-compose.yml` на существующем `GET /api/health` (идея из
  atlas-server) + копирование `VERSION`/`CHANGELOG.md` в образ (нужны
  карточке «О системе»).
- Автобэкап теперь настраивается через `IT_ASSETS_AUTO_BACKUP_ENABLED` и
  `IT_ASSETS_AUTO_BACKUP_INTERVAL_MIN` (идея настраиваемости через env —
  из atlas-server/backupScheduler.js; сама система бэкапов у нас осталась
  своя — ZIP с БД+конфигом, раздельные лимиты по типам). По умолчанию
  поведение не меняется (включено, раз в час).
- Добавлен `.env.example` — раньше все переменные окружения были
  задокументированы только в разрозненных комментариях по коду.
- Добавлен `THIRD_PARTY_LICENSES.md` (идея из atlas-server) — сводка по
  лицензиям npm-зависимостей, сгенерирована по фактически установленному
  дереву `node_modules` (343 транзитивных пакета, все пермиссивные лицензии,
  кроме одного пакета без указанной лицензии в манифесте — не используется
  напрямую нашим кодом).
- `docs/index.html` актуализирован под всё сделанное в Track 0/8: новые
  переменные окружения (×3 таблицы в файле), консольный баннер (обновлён
  пример вывода), структура файлов (VERSION/CHANGELOG.md/THIRD_PARTY_LICENSES.md/
  .env.example/Dockerfile/scripts/), новые эндпоинты `/api/settings/system-info`,
  `/api/settings/version`, `/api/settings/health`, `/api/health` (раньше в
  документации не было вообще), раздел авторизации дополнен объяснением
  `requireLogin` vs `requireAuth`. Заодно исправлено фактически неверное
  утверждение «PORT больше не используется / .env не используется» — оба
  давно используются.
- REF-1: бэкапы и QR-генератор вынесены из `server/index.js` в
  `server/routes/backup.routes.js`/`qr.routes.js` (тот же паттерн, что и
  остальные домены) — `server/index.js` сократился с 727 до 329 строк.
  Логика перенесена 1:1, без изменений поведения.

### Checked
- REF-2: обновление `uuid` 11 → 14 (было в изначальном плане) — **проверено
  и отклонено осознанно**. Начиная с `uuid@12` пакет убрал поддержку
  CommonJS (официальная позиция мейнтейнеров: «For CommonJS codebases, use
  uuid@11»); апгрейд сломает сервер на старте (`ERR_REQUIRE_ESM`, весь наш
  код на `require()`). Замена на `node:crypto.randomUUID()` тоже не
  подходит — генерирует только v4, нам нужен v7 (time-ordered). Остаёмся на
  `11.1.1` — это верное решение, не технический долг.
- REF-3: Docker — статус был неясен, сверено с реальным состоянием
  репозитория. Закрыто через Track 8/INFRA-11 (см. выше).

### Fixed
- Docker-сборка была сломана после INFRA-3: `npm ci` запускался ДО
  копирования `scripts/` в образ, а lifecycle-скрипт `prepare` (git-хук)
  требует `scripts/install-hooks.js` — билд падал бы с "Cannot find
  module". Исправлено флагом `--ignore-scripts` (хуки внутри Docker-образа
  всё равно не нужны — там нет `.git`).
- `.dockerignore` содержал `*.md`, что исключало `CHANGELOG.md` из билд-
  контекста ещё до попытки его скопировать — добавлено точечное
  исключение `!CHANGELOG.md`.

## [beta-1-26w35-01]

### Added
- INFRA-1: `scripts/check-deps-fresh.js` — сравнение mtime `package-lock.json`
  и `node_modules` вместо наивной проверки существования папки в
  `START.bat`/`start.sh`.
- INFRA-3: git pre-commit хук (`scripts/hooks/pre-commit` +
  `scripts/install-hooks.js`) — напоминание обновить `CHANGELOG.md` при
  изменении `server/`/`public/js/`.
- INFRA-4: `scripts/sync-version.js` — синхронизация версии из `package.json`
  в README-бейдж и `docs/index.html` (обнаружил и исправил реальный дрейф:
  docs была на `26w27-02`, актуальная — `26w29-01`).
- INFRA-5: `GET /api/settings/system-info` (admin-only) и
  `GET /api/settings/version` — версия, окружение Node, реально
  установленные зависимости, размер БД, счётчики сущностей, инфо по
  бэкапам; кнопка «Подробная диагностика» в настройках.
- LOC-5/LOC-6: полная локализация RU/EN оставшихся экранов (пользователи,
  типы устройств, генератор инв. номеров, печать QR, глобальный поиск,
  справочники орг/филиалов/локаций, конфиг, общие настройки, бэкапы) +
  финальный проход по всему проекту на предмет пропущенного текста.

- INFRA-6: health-бар в шапке — `GET /api/settings/health` (БД, свежесть
  бэкапа, срок сертификата, свободное место на диске), цветная точка с
  панелью деталей по клику.
- INFRA-8: карточка «О системе» переоформлена по образцу procure-it —
  раздельные блоки «О программе» / «Окружение» (авто-обновление) /
  «Технологии» / «Последние изменения» (из этого файла).

### Fixed
- INFRA-7: критичный пробел в авторизации — GET-роуты активов, истории
  перемещений, справочников (орг/филиалы/локации/типы) и генератора инв.
  номеров были полностью открыты без какой-либо авторизации. Добавлен
  облегчённый `requireLogin` (отдельно от `requireAuth`, который на самом
  деле проверяет режим редактирования и заблокировал бы viewer-роль от
  простого чтения).

### Checked
- INFRA-2: HTTPS-сертификаты (`server/cert.js`) — проверено, доработок не
  требуется (каскад SAN на локальные IP + проверка через X509Certificate +
  HTTP-фолбэк уже реализованы).

## [beta-1-26w29-01] — база на момент начала этой хронологии
- Track 1 (безопасность, SEC-1..10), Track 2 (баг-фиксы, BUG-1..3),
  Track 7 LOC-1..4 (фундамент локализации, активы, CSV-импорт, справочники).
